/**
 * Send thing if authentication succeeds.
 * @param {type} name
 * @returns {undefined}
 */
function sendThing(name){
    console.log("send thing to " + name);
}

/**
 * Send apology if authentication fails.
 * @param {type} name
 * @returns {undefined}
 */
function sendApology(name){
    console.log("say sorry to " + name);
}