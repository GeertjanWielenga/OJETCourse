What you will learn:

Configuration of 'require.js' is done in the 'require.config' section.

How to use this sample:

1. Inclusion of 'require.config' in the 'main.js' file.
2. In NetBeans, explore the options within the require.config section.